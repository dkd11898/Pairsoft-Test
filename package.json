{
  "name": "orangehrm-playwright-assignment",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug",
    "convert:gif": "node ./scripts/convert-to-gif.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "typescript": "^5.5.4",
    "ffmpeg-static": "^5.1.0"
  }
}
